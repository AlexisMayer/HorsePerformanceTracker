/**
 * `@hpt/shared` — future source of truth for cross-package contracts
 * (entity types, API DTOs + Zod schemas, referential enums, pure calc fns).
 *
 * Lot 0.1 ships only this placeholder so the package, its build and its test
 * harness exist. The real contracts arrive with lot 0.2.
 */
export const SHARED_PACKAGE = '@hpt/shared';

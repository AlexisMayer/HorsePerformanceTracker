# Expo/Metro resolve a React Native dependency tree most reliably against a
# flat node_modules. The hoisted linker keeps that compatibility for the app
# while pnpm still manages the workspace.
node-linker=hoisted
